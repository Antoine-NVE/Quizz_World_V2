{% extends 'base.html.twig' %}

{% block title %}{{ category.title }}
  - Niveau
  {{ questionnaire.difficulty }}
{% endblock %}

{% block body %}
  <h1>{{ category.title }}
    - Niveau
    {{ questionnaire.difficulty }}</h1>
  <img class="mb-4" src="{{ asset('img/' ~ category.image) }}" alt="{{ category.title }}">

  <h5>Votre score :
    {{ score }}/10</h5>

  <a class="btn btn-primary" href="{{ path('app_main') }}">Retour à l'accueil</a>

{% endblock %}
