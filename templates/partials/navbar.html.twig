<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_main') }}">Quizz World</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link {{ app.current_route == 'app_main' ? 'active' : '' }}" href="{{ path('app_main') }}">Accueil</a>
        </li>
        {# <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li> #}
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        {% if app.user %}
          {% if app
            .user
            .roles[0] == 'ROLE_ADMIN' %}
            <li class="nav-item">
              <a class="nav-link {{ app.current_route starts with 'admin_' ? 'active' : '' }}" href="{{ path('admin_main_index') }}">Administration</a>
            </li>
          {% endif %}
          <li class="nav-item">
            <a class="nav-link {{ app.current_route starts with 'profile_' ? 'active' : '' }}" href="{{ path('profile_main') }}">Profil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link {{ app.current_route == 'app_login' ? 'active' : '' }}" href="{{ path('app_login') }}">Connexion</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{ app.current_route == 'app_register' ? 'active' : '' }}" href="{{ path('app_register') }}">Inscription</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>